# Hyperion

# Running

## Step 1: The proxy

Go to `hyperion-proxy` and install it with `cargo install --path .`

## Step 2: The event
```bash
brew install just
just debug
```

# Local CI

```
just
```