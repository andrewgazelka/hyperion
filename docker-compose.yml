version: '3.8'

services:
  server:
    build:
      context: .
    ports:
      - "25565:25565"
    environment:
      - RUST_LOG=${RUST_LOG:-info}
    volumes:
      - type: bind
        source: ./profiling
        target: /app/profiling

